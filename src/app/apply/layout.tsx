import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Membership Application | Nav Bihar Renaissance Foundation",
  description: "Apply for NBRF membership — Executive, Corporate, or Honorary. Join Bihar&apos;s pioneering think tank and contribute to research, policy, and development.",
};

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
